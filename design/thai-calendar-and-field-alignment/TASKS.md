# Build Tasks: Thai school calendar and donor ↔ student field alignment

Generated from: design/thai-calendar-and-field-alignment/DESIGN_BRIEF.md
Date: 2026-09-11

Visual direction for every task: **Lumen, unchanged** — no new tokens, calm administrative tone, Figtree + Noto Sans Thai, `success / warning / danger / neutral` tones, 4px spacing, 8px radius. Western years everywhere (`2026–27`); Buddhist Era is out of scope.

Conventions found in the codebase: PascalCase page/component files in `src/modules/<area>/`, SCSS modules beside them, migrations in `supabase/migrations/YYYYMMDDHHMMSS_name.sql`, and no test files yet (Playwright is installed but has no specs). Each task is verified by running `npm run build` and checking the named screen by hand. Where a task says **Verify**, those are the checks.

## Foundation

- [ ] **School calendar settings page**: Migration adds the single-row `programme_settings` (school year 05-16 → 03-31, semester 1 05-16 → 10-10, semester 2 11-01 → 03-31, `report_due_after_days` 21) and the `school_years` view (`school_year_start`, `label` "2026–27", semester dates, due dates, per school with programme fallback). Build `SchoolCalendarSettingsPage` at `/admin/settings/calendar`, reached from the existing Settings nav item next to Branding. It holds date-range rows (day + month) plus the due gap. A live preview line reads *"School year 2026–27: 16 May 2026 – 31 Mar 2027 · Semester 1 report due 31 Oct 2026"*, and invalid ranges are named on the field. **Verify**: defaults load, editing a date updates the preview, saving persists, and an end date before its start is rejected. _Reuses: `PageHeader`, `FormSection`, `Button`, `InlineMessage`, Mantine inputs. New page._

- [ ] **Per-school calendar override**: Migration adds nullable overrides to `schools` for the six dates and `report_due_after_days`. In `SchoolForm`, remove the *Reporting period* select and add a collapsed *School calendar* section. It reads "Uses programme dates", and *Use different dates* reveals the fields prefilled from the programme. `AdminSchoolDetailPage` shows which dates apply, replacing the "Reporting period" row. `reporting_period_months` stops being read. Schools whose value was not 6 are recorded for the migration review (last task). **Verify**: override one school, and the `school_years` view returns its dates while other schools keep the defaults. _Modifies: `SchoolForm`, `AdminSchoolDetailPage`, `schoolProfile.ts` (`REPORTING_PERIODS` retired). Depends on: School calendar settings page._

- [ ] **Language-aware dates**: `formatDate` / `formatDateTime` in `format.ts` take the current i18n language (or an explicit one for emails) and render `12 Sep 2026` in English and Thai month abbreviations with a Western year in Thai (`12 ก.ย. 2026`). Add `formatSchoolYear` ("2026–27"). Repoint every raw `toLocaleDateString()` / `Intl.DateTimeFormat` call in the 18 files found in the audit: `AdminDonorsPage`, `AdminEditDonorPage`, `AdminReportsBetaPage`, `AdminScholarshipsPage`, `AdminSchoolDetailPage`, `AdminSchoolsPage`, `AdminStudentDetailPage`, `AdminStudentOverviewPage`, `AdminStudentsPage`, `AdminTeacherOverviewPage`, `AdminTeachersPage`, `AdminUsersRolesPage`, `schoolProfile.ts`, `DonorDashboardPage`, `DonorStudentDetailPage`, `ReportList`, `TeacherDashboardPage`, `TeacherStudentsPage`. **Verify**: `grep -rE "toLocaleDateString|new Intl.DateTimeFormat" src` returns only `format.ts`; flipping `LanguageSwitch` changes month names with no reload and no table reflow. _Modifies: `format.ts` and the listed pages._

## Core UI

- [ ] **Semester report cycle** _(highest risk, so it comes before the rest of Core UI)_: Rewrite `reportingCycle.ts` and `reportStatus.ts` to read cycles from `school_years`: `{ schoolYearStart, semester, start, end, dueOn }`, keys `2026-S1`, labels *Semester 1 · 2026–27*. `CYCLE_MONTHS` retires. Migration adds `term_updates.school_year_start` and `semester`, backfilled from `report_date` against the student's school dates, and sets `due_date` to the semester due date. Repoint the duplicate mid-year rule at `AdminSchoolDetailPage.tsx:65`. Update the teacher dashboard, teacher students list, admin reports pages and the report email template to show semester labels. **Verify**: a report dated 12 Jun 2026 shows *Semester 1 · 2026–27*; a student with no report this semester shows due 31 Oct 2026, or its school's override; due soon and overdue still fire 30 days before and after. _Modifies: `reportingCycle.ts`, `reportStatus.ts`, `TeacherDashboardPage`, `TeacherStudentsPage`, `AdminReportsPage`, `AdminReportsBetaPage`, `AdminSchoolDetailPage`, `supabase/functions/report-email/template.ts`. Depends on: School calendar settings page, Per-school calendar override._

- [ ] **Due dates follow calendar edits**: Saving the settings page or a school override recalculates `due_date` on open (not approved) reports for the affected schools, then shows *"Due dates updated for 186 open reports."* **Verify**: move semester 1's end by a week, and open reports' due dates move with it while approved reports are untouched. _Modifies: `SchoolCalendarSettingsPage`, `SchoolForm`. Depends on: Semester report cycle._

- [ ] **Bilingual donor card**: Migration adds `students.donor_display_name_th` / `donor_description_th` and moves Thai-script content out of the English columns (by Unicode range). In `DonorProfileTab`, display name and description become `BilingualField`s (EN · ไทย tablist, *Missing* marker, 420-char counter per language), and the preview card gets a language toggle. Publishing needs one complete language. `sendBlockedReason` and `send-donor-card` block a send to a reader whose language is missing and name it: *"Khun Somchai reads Thai. This student's Thai description is empty."* **Verify**: an English-only card publishes; sending to a Thai-reading donor is blocked with that sentence; writing Thai unblocks it. _Reuses: the `BilingualField` pattern from the email brief (create it here if it does not exist yet). Modifies: `DonorProfileTab`, `studentProfile.ts`, `donorCard.ts`, `send-donor-card`._

- [ ] **Need and grant type have one home**: Migration renames `grant_types.amount_per_period` → `monthly_amount_thb`, retires `default_duration_months` and `scholarships.grant_type_id`, and maps `students.scholarship` free text to `grant_types` by name. Unmatched values are recorded for review. In `StudentFields`, remove the *Scholarship* text field; choosing a *Grant type* prefills *Monthly support expected* only when it is empty. In the grant type create and edit pages, relabel the amount *Monthly amount (THB)* and remove *Default duration*. Anything still reading `students.scholarship` (the student overview KPI, `AdminSchoolDetailPage`) falls back to the grant type name. **Verify**: picking a grant type fills ฿1,500 on an empty student; a typed ฿1,800 survives changing the grant type; editing the grant type's amount changes no existing student. _Modifies: `StudentFields`, `StudentForm`, `AdminCreateGrantTypePage`, `AdminEditGrantTypePage`, `AdminGrantTypesOverviewPage`, `AdminStudentOverviewPage`, `AdminSchoolDetailPage`._

- [ ] **One email switch per donor, one per sponsorship**: On both donor forms, label `wants_email_updates` *Send report emails* and keep *Wants newsletter* separate; remove any `email_opt_out` references if the records-brief work added them. Extend `report_email_payload` (on top of `20260911090000_report_recipients_email_pref.sql`) to require `scholarships.report_emails_enabled` too, returning an `excluded_reason` per sponsor. `EmailPreviewDialog`'s excluded list shows *"Khun Somchai has turned off report emails."* **Verify**: turning the donor switch off excludes them from every report send, with the reason; turning it back on restores the previous per-student settings. _Modifies: `AdminCreateDonorPage` / `AdminEditDonorPage` (or `DonorForm` if the records brief landed), `EmailPreviewDialog`. The `SponsorsTab` disabled-row message is built in the sponsorship task list._

- [ ] **Donor email and currency single source**: The send path and duplicate check read `donors.contact.email` only. On the donor detail page, when a linked `profiles.email` differs, show *"Signs in as x@…; reports go to y@…"* as a neutral `InlineMessage`. The donor page's currency comes from `donor_contributions.currency` (falling back to THB), not `scholarship_awards`. **Verify**: a donor whose login and contact emails differ shows the line, and the report goes to the contact email; `grep scholarship_awards src/modules/admin/AdminEditDonorPage.tsx` returns no currency read. _Modifies: `AdminDonorDetailPage`, `AdminEditDonorPage`, `supabase/functions/report-email`._

- [ ] **Grade promotion review**: Migration adds `student_grade_history` (`school_year_start`, `grade_level`, `school_id`, `outcome`, `confirmed_by`, `confirmed_at`, change trail), `students.status` value `'graduated'`, and maps legacy grades (`4`, `Grade 4`, `ป.4`) onto `GRADES`, seeding one history row per enrolled student. Build `GradePromotionPage`, grouped by school. Each row shows current grade → proposed grade (P6 → M1, M6 → Graduate) and an outcome select (Promote / Repeat / Graduate / Moved school / Left, with a school picker for Moved school). Unmappable grades are flagged at the top. The page has **Confirm school** and **Confirm all**, and a summary: *"214 students updated · 12 graduating · 3 repeating"*. It is open 1 May – 30 June. A `TodayRail` item appears from 1 May: *"School year 2026–27 starts 16 May: review grades (214 students)"*. **Verify**: confirming a school writes history rows and updates `grade_level`; the page is read-only outside the window but reachable. _Reuses: `DataTable`, `Select`, `Button`, `Badge`, `TodayRail`. New page. Modifies: `dashboardMetrics.ts`, `schoolProfile.ts`._

- [ ] **Grade history on the student**: Add `GradeHistoryList` to `AdminStudentOverviewPage`: one line per school year (*2025–26 · P4 · Promoted*). An admin edit action opens a small `FormDrawer` to correct the grade or outcome at any time, including after confirmation and after 30 June. It saves who changed it and when, and updates `students.grade_level` when the current year is edited. **Verify**: correcting a confirmed grade updates both the history line and the header grade, and the change shows who made it. _New component. Depends on: Grade promotion review._

- [ ] **Graduation at year end**: A scheduled edge function runs on 31 Mar, UTC, outside 1–3 AM. It sets students confirmed as *Graduate* to `status = 'graduated'` and writes a `student_events` entry. Where the sponsorship work has landed, it also moves their active sponsorships to `needs_decision` with reason *"Mali has graduated"*. Otherwise it only archives. Graduated students leave working lists but show a *Graduated* success badge on their record, not the archived banner. **Verify**: run the function against a test date, and graduating students change status and badge while others are untouched. _Modifies: `AdminStudentOverviewPage`, `AdminStudentsPage` status filter. Depends on: Grade promotion review; the Needs decision step depends on the donor-sponsorship tasks._

## Interactions & States

- [ ] **Migration review panel**: Collect every conflict the migrations above recorded into one admin view, `MigrationReviewPanel`: unmapped grades, unmatched scholarship text, schools with non-6 reporting periods, and donors whose contact, invite and login emails differ. Each is a plain sentence with an inline fix control. A dashboard banner reads *"The update found 17 records that need a decision."* and disappears when the list is empty. Covers: loading, empty ("Nothing left to review"), a fix-save error, and a row disappearing once fixed. **Verify**: seed one conflict of each kind, fix each in place, and the banner goes away. _Reuses: `Banner`, `DataTable`, `InlineMessage`, `EmptyState`. New component. Depends on: every Foundation and Core UI migration task._

- [ ] **States on the new screens**: Settings page: loading, save error, and the "due dates updated" confirmation. Promotion page: before 1 May (review not open yet, with the date), after 30 June (read-only with a link to per-student edits), an empty school, and a confirm error. Bilingual card: both-missing, one-missing and send-blocked. Grade history: no history yet. Covers: loading, empty, error, success, and the out-of-window states listed. _Modifies: `SchoolCalendarSettingsPage`, `GradePromotionPage`, `DonorProfileTab`, `GradeHistoryList`._

## Responsive & Polish

- [ ] **Responsive pass**: Settings page ranges stack on mobile with the preview as a top card. Promotion page: a table on desktop, sticky school headers on tablet, and on mobile one row per student with a tap-to-change outcome sheet and *Confirm school* pinned at the foot. `BilingualField` keeps its tablist at every width. Dates don't reflow tables when the language switches. Breakpoints: ≥1024px, 641–1023px, ≤640px.

- [ ] **Accessibility pass**: `<html lang>` follows the interface. Semester labels are words, never "S1". Date-range inputs have explicit start/end labels, and invalid ranges are named on the field. Each promotion outcome select is labelled with the student's name. Flagged rows state their reason in text. After *Confirm school*, focus moves to the next school heading and a live region announces the count. "2026–27" is announced as "2026 to 2027". The bilingual *Missing* state is announced. Migration review conflicts are sentences with labelled fix controls, and nothing relies on colour. WCAG 2.1 AA contrast using existing tokens.

## Review

- [ ] **Design review**: Run /design-review against `design/thai-calendar-and-field-alignment/DESIGN_BRIEF.md`.
